**ALWAYS**
- Use `bun` for any package installation, run scripts, and more.
- Follow the styles, color schemes, spacings, etc. from the current theme.
- Use `bunx` commands for new shadnc components. Example: `bunx --bun shadcn@latest add button-group`.
- Follow Tanstack Query conventions and best practices for data fetching and caching.
- Follow Tanstack Start conventions and best practices for building end-to-end features.

**NEVER**
- Write any test cases.
